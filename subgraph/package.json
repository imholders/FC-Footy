{
  "name": "score-square-v1",
  "version": "0.1.0",
  "private": true,
  "description": "Subgraph for FC Footy ScoreSquare contract on Base network",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy score-square-v1 --node https://api.studio.thegraph.com/deploy/ --ipfs https://api.studio.thegraph.com/ipfs/",
    "create-local": "graph create --node http://localhost:8020/ score-square-v1",
    "remove-local": "graph remove --node http://localhost:8020/ score-square-v1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 score-square-v1"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.96.0"
  }
}
