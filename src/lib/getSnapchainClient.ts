/* import { HubRestAPIClient } from '@standard-crypto/farcaster-js';

export interface SnapchainClientConfig {
  logger?: any;
}

export function getSnapchainClient(configOverrides?: SnapchainClientConfig): HubRestAPIClient {
  return new HubRestAPIClient({
    hubUrl: 'https://snapchain-api.neynar.com',
    ...(configOverrides || {}),
  });
} */